$(document).ready(function () {
  $('.beaten-path').slick({
    dots: true,
    infinite: true,
    speed: 500,
    slidesToShow: 1,
    adaptiveHeight: true,
  });
});

$(document).ready(function () {
  $('.dev-tracker').slick({
    dots: true,
    infinite: true,
    speed: 500,
    slidesToShow: 1,
    adaptiveHeight: true,
  });
});

